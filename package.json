{
  "name": "my-react-app",
  "version": "1.0.0",
  "description": "A sample React application",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",   // Command to start the development server
    "build": "react-scripts build",   // Command to build the production bundle
    "test": "react-scripts test",     // Command to run tests
    "eject": "react-scripts eject"    // Command to eject from create-react-app setup (if needed)
  },
  "keywords": [
    "react",
    "frontend",
    "web"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.2",               // React library
    "react-dom": "^17.0.2",           // React DOM library
    "react-scripts": "4.0.3",         // Create React App scripts
    // Add other dependencies your project needs
  },
  "devDependencies": {
    // Add development dependencies here, if any
  }
}
